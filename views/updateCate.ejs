<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dropdown with Redirection</title>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
    rel="stylesheet"
/>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #333;
            padding: 10px;
        }
        .navbar a {
            color: white;
            padding: 14px 20px;
            text-decoration: none;
            text-align: center;
        }
        .navbar a:hover {
            background-color: #ddd;
            color: black;
        }
        .navbar .menu {
            display: flex;
            flex-wrap: wrap;
        }
        .navbar .menu a {
            flex: 1;
        }
        .navbar .menu-icon {
            display: none;
            cursor: pointer;
        }
        @media (max-width: 600px) {
            .navbar .menu {
                display: none;
                flex-direction: column;
                width: 100%;
            }
            .navbar .menu a {
                text-align: left;
                padding: 10px;
                border-top: 1px solid #ddd;
            }
            .navbar .menu-icon {
                display: block;
            }
        }
        .form-container {
            padding: 20px;
        }
        .form-container form {
            max-width: 400px;
            margin: 0 auto;
        }
        .form-container label {
            display: block;
            margin-bottom: 8px;
        }
        .form-container input[type="text"],
        .form-container input[type="submit"],
        .form-container select {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .hidden-link {
            display: none;
        }
    </style>
    <style>
        .dropdown {
            position: relative;
            display: inline-block;
            width: 200px;
        }
        .dropdown-button {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            background-color: #f9f9f9;
            cursor: pointer;
            text-align: center;
        }
        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 100%;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            z-index: 1;
            border: 1px solid #ccc;
            border-radius: 4px;
            max-height: 200px;
            overflow-y: auto;
        }
        .dropdown-content div {
            padding: 10px;
            cursor: pointer;
        }
        .dropdown-content div:hover {
            background-color: #f1f1f1;
        }
        .show {
            display: block;
        }
    </style>
    <script>
        function toggleMenu() {
            var menu = document.getElementById("menu");
            if (menu.style.display === "block") {
                menu.style.display = "none";
            } else {
                menu.style.display = "block";
            }
        }

        function redirectToCategory() {
            var select = document.getElementById("categoryDropdown");
            var selectedValue = select.value;
            console.log(selectedValue);
            if (selectedValue) {
                var categoryLink = document.getElementById("categoryLink");
                categoryLink.href = "/addSub/" + selectedValue;
                categoryLink.click();
            }
            console.log("Redirected");
        }
    </script>
     <script>
        
    </script>
</head>
<body>


    <div class="form-container">
        <form method="post" action="/updated/<%=cate._id%>">
            <label for="category">Category:</label>
            <input type="text" id="category" name="category" required placeholder="Add category" value="<%=cate.categoryName%>">
            <input type="submit" value="Submit">
        </form>
    </div>
</body>
</html>
